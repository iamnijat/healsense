class Durations {
  const Durations._();

  static const twoHundredMillisecondsDuration = Duration(milliseconds: 200);
  static const threeHundredMillisecondsDuration = Duration(milliseconds: 300);
  static const sixHundredMillisecondsDuration = Duration(milliseconds: 600);
  static const eightHundredMillisecondsDuration = Duration(milliseconds: 800);

  static const oneSecondDuration = Duration(seconds: 1);
  static const sevenSecondsDuration = Duration(seconds: 7);
}
